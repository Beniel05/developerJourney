true
false

let isLoggedIn = true;

if(isLoggedIn) console.log("The User is Logged In");
else console.log("The User is NOT Logged In");

isLoggedIn = false;

if(isLoggedIn) console.log("The User is Logged In");
else console.log("The User is NOT Logged In");

// Some random boolean use cases;
let isPrime = true;
let canDrive = false;
let isSingle = true;

// Dynamic Typed Language - JS
// isLoggedIn = 48971239; // A boolean variable can also store any value.