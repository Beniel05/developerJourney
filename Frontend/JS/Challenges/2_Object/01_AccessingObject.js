const product = {
    name: "Wireless Mouse",
    brand: "Logitech",
    price: 899,
    inStock: true
};

let details = `${product.brand} ${product.name} costs ₹${product.price}. In Stock: ${product.inStock}`;

console.log(details);